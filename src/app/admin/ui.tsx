"use client";

export default function AdminUI() {
  return (
    <div style={{ padding: 40 }}>
      <h1>Admin UI Loaded</h1>
      <p>If you see this, client components are working.</p>
    </div>
  );
}
